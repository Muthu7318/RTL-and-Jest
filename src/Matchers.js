import React from "react";

function Matchers(props) {
  return (
    <div>
      <button>Go back</button>
      <form aria-label="form">
        <button>Save</button>
        <button>Cancel</button>
      </form>
    </div>
  );
}

export default Matchers;
